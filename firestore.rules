rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to sessions
    match /sessions/{sessionId} {
      allow read, write: if true;
    }
  }
}